<nav class="fixed-top navbar navbar-expand-lg navbar-light shadow-lg">
  <div class="container-fluid mx-xl-5">
    <a class="navbar-brand text-decoration-none fs-1" href="/">Keebo</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div id="navbarSupportedContent" class="collapse navbar-collapse">
      <div class="mx-auto">
        <ul class="navbar-mid navbar-nav text-uppercase hstack gap-5">
          <li class="navbar-item">
            <a class="navbar-link text-decoration-none fs-6" href="/">Home</a>
          </li>
          <li class="navbar-item">
            <a class="navbar-link text-decoration-none fs-6" href="/layouts">Layout</a>
          </li>
          <li class="navbar-item">
            <a class="navbar-link text-decoration-none fs-6" href="/A">Board</a>
          </li>
        </ul>
      </div>
    </div>
    <%if account_signed_in? %>
      <%= link_to "Sign Out", destroy_account_session_path, data: { "turbo-method": :delete } %>
      <a class="nav-link" href="<%= cart_index_path %>">
        <i class="fas fa-shopping-cart fs-5">
            <% if session[:shopping_cart].any? %>
                <sup class="text-white rounded-1 bg-danger pb-1 px-2 "><%= session[:shopping_cart].count %></sup>
            <% end %>
        </i>
      </a>
    <%else%>
      <a href="/accounts/sign_in">Sign In</a>
    <%end%>
  </div>
</nav>